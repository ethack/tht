% general

# Skip first line or header row #tip
tail -n+2

# Read file line by line
while IFS= read -r line; do echo "$line"; done < "<inputfile>"

# Go to a nested directory (e.g. /host/opt/zeek/logs) #tip
g logs

# Go to the host filesystem #tip
g /host

# Modern alternative to nano #tip
micro

